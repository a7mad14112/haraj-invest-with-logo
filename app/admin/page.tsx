import Link from "next/link";import { Navbar } from "@/components/layout/Navbar";import { Footer } from "@/components/layout/Footer";import { AdminShell } from "@/components/admin/AdminNav";import { projects,reports,users,messages } from "@/data/mock";import { FolderKanban,Flag,UsersRound,MessageCircle } from "lucide-react";
const metrics=[['مشاريع قيد المراجعة',projects.filter(p=>p.status==='pending').length,'/admin/projects',FolderKanban],['بلاغات قيد المراجعة',reports.filter(r=>r.status==='pending').length,'/admin/reports',Flag],['إجمالي المستخدمين',users.length,'/admin/users',UsersRound],['رسائل جديدة',messages.filter(m=>m.status==='new').length,'/admin/contact-messages',MessageCircle]] as const;
export default function Page(){return <main><Navbar/><section className="bg-white py-14"><div className="mx-auto max-w-7xl px-5"><h1 className="text-4xl font-black">مؤشرات حراج الفرص والمشاريع</h1><p className="mt-3 text-slate-600">نظرة تنفيذية على المنصة.</p></div></section><AdminShell><div className="grid gap-5 md:grid-cols-2">{metrics.map(([t,v,h,Icon])=><Link href={h} key={h} className="rounded-[2rem] border bg-white p-6 shadow-sm"><Icon className="mb-4 h-8 w-8"/><div className="text-3xl font-black">{v}</div><div className="mt-2 font-black">{t}</div></Link>)}</div></AdminShell><Footer/></main>}
